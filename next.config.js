/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'images.pexels.com',
      'images.pixabay.com',
      'cdn.pixabay.com',
      'raw.githubusercontent.com'
    ],
  },
}

module.exports = nextConfig 